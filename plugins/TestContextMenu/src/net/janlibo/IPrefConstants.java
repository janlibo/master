package net.janlibo;

public interface IPrefConstants {

	final String JENKINS_HOST = "jenkins.host";

	final String JENKINS_PORT = "jenkins.port";

	final String JOB_NAME_WIN = "win.job.name";

	final String JOB_NAME_MAC = "mac.job.name";

	final String ARTIFACT_NAME = "build.name";

	final String JENKINS_USERNAME = "jenkins.username";

	final String JENKINS_PASSWORD = "jenkins.password";

	final String JOB_LAST_SELECTION_WIN = "last.selection.win";

	final String JOB_LAST_SELECTION_MAC = "last.selection.mac";

}
